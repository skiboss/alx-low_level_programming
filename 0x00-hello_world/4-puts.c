#include <stdio.h>

    /**
     * main - Entry point
     *
     * Return: Always 0 (Success)
     */
int main(void) /* this function prints the string in the puts() function*/ 
{
	puts("Programming is like building a multilingual puzzle\n");

	return (0);
}
