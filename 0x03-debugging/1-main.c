#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("Indifinite loop incoming :(\n");

	i = 0;

	/* while (i < 10)
	{
		putchar(i);
	} */

	printf("Indifinite loop avoided! \\o/\n");

	return (0);
}
