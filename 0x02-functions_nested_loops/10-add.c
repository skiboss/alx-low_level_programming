#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * add - Funtion that sums up two integers
 * @a: first integer argument
 * @b: second integer argument
 * Return: This returns the sum of the two numbers
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
