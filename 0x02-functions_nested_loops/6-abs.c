#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _abs - inbuilt function to convert argument to its absolute value
 *
 * Return: 0 (Success)
 */
int _abs(int x)
{
	int result;

	result = abs(x);
	putchar(x);
}
