#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _abs - inbuilt function to convert argument to its absolute value
 * @x: argument input
 * Return: 0 (Success)
 */
int _abs(int x)
{
	int x;

	return (abs(x));
}
